# RT 키 그룹

keyboard.getRtKeyGroup() — RT 키 그룹 가져오기 (0xFE)
keyboard.setRtKeyGroup(params) — RT 키 그룹 설정

```javascript
const result = await keyboard.getRtKeyGroup();
await keyboard.setRtKeyGroup({ keyName: 'A', group: 1 });
```
