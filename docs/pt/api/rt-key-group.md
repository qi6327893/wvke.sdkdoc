# Grupo de Teclas RT

keyboard.getRtKeyGroup() — Grupos RT (0xFE)
keyboard.setRtKeyGroup(params) — Definir grupos RT

```javascript
const result = await keyboard.getRtKeyGroup();
await keyboard.setRtKeyGroup({ keyName: 'A', group: 1 });
```
