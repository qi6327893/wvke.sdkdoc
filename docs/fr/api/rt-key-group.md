# Groupe de Touches RT

keyboard.getRtKeyGroup() — Groupes RT (0xFE)
keyboard.setRtKeyGroup(params) — Définir groupes RT

```javascript
const result = await keyboard.getRtKeyGroup();
await keyboard.setRtKeyGroup({ keyName: 'A', group: 1 });
```
