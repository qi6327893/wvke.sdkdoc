# Grupo de Teclas RT

keyboard.getRtKeyGroup() — Obtener grupos de teclas RT (0xFE)
keyboard.setRtKeyGroup(params) — Establecer grupos de teclas RT

```javascript
const result = await keyboard.getRtKeyGroup();
await keyboard.setRtKeyGroup({ keyName: 'A', group: 1 });
```
