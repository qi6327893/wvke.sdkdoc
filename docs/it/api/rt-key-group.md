# Gruppo Tasti RT

keyboard.getRtKeyGroup() — Gruppi RT (0xFE)
keyboard.setRtKeyGroup(params) — Imposta gruppi RT

```javascript
const result = await keyboard.getRtKeyGroup();
await keyboard.setRtKeyGroup({ keyName: 'A', group: 1 });
```
