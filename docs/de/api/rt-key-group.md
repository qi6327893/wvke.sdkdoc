# RT-Tastengruppe

keyboard.getRtKeyGroup() — RT-Tastengruppen abrufen (0xFE)
keyboard.setRtKeyGroup(params) — RT-Tastengruppen setzen

```javascript
const result = await keyboard.getRtKeyGroup();
await keyboard.setRtKeyGroup({ keyName: 'A', group: 1 });
```
