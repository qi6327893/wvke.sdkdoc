# RT Группа Клавиш

keyboard.getRtKeyGroup() — Группы RT (0xFE)
keyboard.setRtKeyGroup(params) — Установить группы RT

```javascript
const result = await keyboard.getRtKeyGroup();
await keyboard.setRtKeyGroup({ keyName: 'A', group: 1 });
```
